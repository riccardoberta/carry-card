# Local Python environment — not shared between machines.
.venv/
__pycache__/
*.pyc
.pytest_cache/
*.egg-info/

# Your own Klarna screenshots and generated output. These may contain
# personal loyalty-card data (never credentials) — keep them local.
input/
preview.json
klarna-migration-report.txt
