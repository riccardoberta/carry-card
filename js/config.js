// Google OAuth Web Client ID — safe to publish (it identifies the app, it's not a
// secret; the client secret is never used since this app only does the client-side
// token flow). Create yours in Google Cloud Console → APIs & Services → Credentials
// → Create Credentials → OAuth Client ID → Web application, with this site's origin
// under "Authorized JavaScript origins". See README.md for the full walkthrough.
export const GOOGLE_CLIENT_ID = "624965660580-0i5acjt14l6gs7ogc86rt5bsumvni1k4.apps.googleusercontent.com";
